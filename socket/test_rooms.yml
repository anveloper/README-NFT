config:
  target: "http://localhost:5000"
  phases:
    - duration: 10
      arrivalRate: 200
scenarios:
  - name: Create room event
    engine: socketio
    flow:
      - emit:
          chennel: enter_room
          data: 
            - userAddress: asdf
              nickname: asdf
              roomTitle: asdf 
              done: asdf